/**
 * Temporarily hides transcription-driven features without removing their implementation.
 * Keep this false until the local Whisper pipeline is ready to ship again.
 */
export const TRANSCRIPTION_FEATURE_ENABLED = false;
